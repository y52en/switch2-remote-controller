[project]
name = "switch2-remote-controller-tools"
version = "0.1.0"
description = "Reproducible firmware tooling for switch2-remote-controller"
requires-python = "==3.12.*"
dependencies = [
  "esptool==5.3.1",
  "pip==26.2.1",
  "platformio==6.1.19",
]

[tool.uv]
package = false
